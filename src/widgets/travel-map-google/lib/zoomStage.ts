/**
 * 지도 줌 단계.
 * 2.5는 2(시군구 색칠만)와 3(전 지역 [+]·지역명) 사이의 중간 단계로,
 * 인기지역([POPULAR_REGIONS])의 [+] 버튼·지역명만 먼저 노출한다.
 * 숫자 리터럴이라 기존 단계 비교(>= 2, < 3 등)는 2단계와 동일하게 동작한다.
 */
export type ZoomStage = 0 | 1 | 2 | 2.5 | 3
